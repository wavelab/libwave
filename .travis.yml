dist: trusty
sudo: required

language:
    - c++

cache:
    ccache: true

install:
    - sudo bash scripts/install/install_deps.bash

script:
    - bash scripts/ci_run.bash

notifications:
    email: false